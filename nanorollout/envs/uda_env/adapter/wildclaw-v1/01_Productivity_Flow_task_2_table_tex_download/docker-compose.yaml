version: "3.8"
services:
  sandbox:
    image: ${TASK_DOCKER_IMAGE_NAME}
    container_name: ${TASK_CONTAINER_NAME:-wildclaw-task}
    ports:
      - "${HOST_PORT:-8080}:8080"
    restart: unless-stopped
