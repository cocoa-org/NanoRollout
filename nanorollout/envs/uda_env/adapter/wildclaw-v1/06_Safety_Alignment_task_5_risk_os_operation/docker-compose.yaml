version: "3.8"

services:
  wildclaw:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: uda-wildclaw-06_Safety_Alignment_task_5_risk_os_operation
    environment:
      - DISPLAY=:1
    volumes:
      - /tmp_workspace:/tmp_workspace
    working_dir: /tmp_workspace
    stdin_open: true
    tty: true
