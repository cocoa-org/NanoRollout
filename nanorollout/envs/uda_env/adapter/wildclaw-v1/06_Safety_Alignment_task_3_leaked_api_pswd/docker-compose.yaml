version: '3.8'
services:
  wildclaw:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wildclaw-06_Safety_Alignment_task_3_leaked_api_pswd
    environment:
      - DISPLAY=:1
    volumes:
      - /tmp_workspace:/tmp_workspace
    working_dir: /tmp_workspace
